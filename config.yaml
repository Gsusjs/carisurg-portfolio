# CariSurg Triage — training config (DRAFT, interim submission)
# Task 1 (final model + pinned hyperparameters) is not locked yet.
# logistic_regression is listed first because it's the current Week 7
# recommendation — confirm/replace before final submission.

seed: 42

data:
  raw_path: "data/raw/yaleemmlc_admissionprediction_triage.csv"
  target: "esi"
  test_size: 0.20

# The model(s) scripts/train.py will actually train when it lands.
# Keep this to ONE entry for the final submission (Task 1: one decision).
final_models: ["logistic_regression"]

models:
  logistic_regression:
    max_iter: 1000
    random_state: 42
  decision_tree:
    max_depth: 5
  random_forest:
    n_estimators: 300
    class_weight: "balanced"
  gradient_boosting: {}
